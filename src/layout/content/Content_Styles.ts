import styled from 'styled-components';

const Content = styled.div``

const ScreensWrapper = styled.div`
    max-width: 911px;
    width: 100%;
    padding-top: 16px;
`

export const S = {
    Content,
    ScreensWrapper,
}