import styled from 'styled-components';

const Header = styled.header`
    background-color: #ffffff;
    padding: 10px 0;
    height: 50px;
    position: sticky;
    top: 0; 
    left: 0;
    right: 0;
    z-index: 1;
`

export const S = {
    Header
}